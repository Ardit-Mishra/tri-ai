# Tri-AI Handoff — 2026-09-21

## Objective

Make Tri-AI a local-first, verification-gated agent operating system for
software, research, and business workflow delivery. A task is accepted only
when its recorded verifier exits successfully; an agent report is never
acceptance evidence.

## Completed in `codex/freellm-router`

- Extended the diagnostic routing policy from one fallback to an ordered chain:
  `OmniRoute :20128/v1 -> FreeLLMAPI :3001/v1 -> Ollama :11434/v1`.
- Preserved the existing safety rule: only `429`, timeout, and connection
  errors advance the probe. Other HTTP errors stop the chain. The probe cannot
  import the board, worker, or executor, and cannot accept work.
- Added `scripts/install-freellmapi.ps1`, which installs the reviewed upstream
  source in `~/.tri-ai/freellmapi`, pins it to
  `b882473c3a23251be312a7270e2e0dc1eae1329d`, binds it to loopback, and never
  writes provider credentials into this repository.
- Added a Claude Code / Codex / Hermes configuration helper. It defaults to
  `--dry-run`; applying client configuration requires the operator's local
  `FREELLMAPI_API_KEY` environment variable.
- Added `docs/FREELLMAPI.md` and `docs/CAPABILITY_CATALOG.md`.
- Added a dynamic capability catalog covering installed skills, ECC/GSD,
  cloned sources, MCP servers, and all requested adapters. New tasks infer
  governed methods and receive task-specific concrete tool matches.
- Added a technology radar with GitHub, Hacker News, and optional YouTube
  discovery; safe archive extraction; static malware-risk signals; and
  Docker-only, no-network candidate probes. Reports are machine-readable and
  visible through Telegram `/radar`.
- Added Telegram `/capabilities` and `/radar` read-only status commands.

## Local operational state

- FreeLLMAPI was built and launched at `http://127.0.0.1:3001`.
- `GET /v1/models` correctly returned `401` without a unified key. This proves
  the local router is listening and rejects anonymous use; it does *not* prove
  any provider quota or model route is configured.
- The upstream `npm ci` report showed 13 dependency vulnerabilities (1 low, 6
  moderate, 6 high) at the pinned revision. Do not run `npm audit fix` blindly;
  review the upstream advisory tree and release notes first.
- The upstream README describes roughly 7.4B listed free-tier tokens per month
  at this revision. Treat that as an external catalog estimate, never a
  guaranteed Tri-AI resource or portfolio claim.

## Verification

```text
python -m unittest tests.test_routing_probe -v  -> 8 passed
python tests/run.py                              -> 641 passed in 272.749s
```

Latest repository-wide result after the capability catalog, radar, readiness,
Telegram status, Brain, and HUD work:

```text
python tests/run.py -> 688 passed in 260.739s
```

## Remaining operator action

1. Open the local FreeLLMAPI dashboard and add only provider accounts/keys the
   operator owns. Create one unified key.
2. Store that unified key in the user environment as `FREELLMAPI_API_KEY`.
3. Run `scripts/configure-freellmapi-clients.ps1` and inspect its dry-run;
   apply only after confirming the generated client configuration.
4. Fill an out-of-repo routing policy with real model IDs, then deliberately
   preflight each route and retain its evidence before treating FreeLLMAPI as
   an eligible execution fallback.
5. Register `scripts/register-technology-radar.ps1` only after this branch is
   merged or moved to its durable checkout; a scheduled task should not point
   at a disposable worktree.
6. Candidate tools remain cataloged even when missing or lightly adopted.
   Promote one only after quarantine evidence, license review, an adapter with
   permission scopes, and a task-level verification contract; see
   `docs/CAPABILITY_REVIEW_2026-09-21.md` and `docs/TECHNOLOGY_RADAR.md`.
