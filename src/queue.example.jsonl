// Continuity queue — work the free lane can execute AND self-verify.
// Each task needs a `verify` command whose exit code decides pass/fail.
// If you cannot write that command, the task does not belong here.
// Run:  python run_queue.py    (--dry-run to preview)
{"id": "gs-tests", "title": "GenomeSight: full test suite must stay green", "repo": "<repo-path>/GenomeSight", "branch": "ui/instrument-design-system", "prompt": "Run the GenomeSight test suite and report the exact final summary line verbatim.\n\nCommand: uv run --python 3.12 --with pytest -m pytest -q\n\nIf any test fails, read the failure, fix the CODE (not the test, unless the test is provably asserting something false), and re-run until green. If you cannot fix it, say so plainly and change nothing.", "verify": "uv run --python 3.12 --with pytest -m pytest -q", "verify_timeout": 900, "timeout": 1800}
{"id": "bs-tests", "title": "BioStudio: full test suite must stay green", "repo": "<repo-path>/Ardit-BioStudio", "branch": "ui/instrument-design-system", "prompt": "Run the BioStudio test suite and report the exact final summary line verbatim.\n\nCommand: uv run --python 3.12 --with-requirements requirements.txt --with-requirements requirements-dev.txt -m pytest tests/ -q\n\nIf any test fails, read the failure and fix the CODE. Two tests compare README tables against models/saved_models/admet_models_manifest.json \u2014 if those fail, the README numbers are stale and must be updated to match the manifest exactly. Never edit the manifest to match the README; the manifest is generated from real training runs and is the source of truth.", "verify": "uv run --python 3.12 --with-requirements requirements.txt --with-requirements requirements-dev.txt -m pytest tests/ -q", "verify_timeout": 1200, "timeout": 2400}
